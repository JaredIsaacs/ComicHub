# Notes for future

idk if we really need a table for most junction classes, excluding Comic_Source since it has specialized fields. The others are simply ways to connect other tables. 

Honestly thinking of changing the structure of the database modules. Instead of fragmented files, making a monolithe for better documenation management?

Users are probably gonna need docker installed to get the image storage working.